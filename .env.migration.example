# Supabase Migration Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================================================
# SUPABASE CREDENTIALS (for data export)
# ============================================================================
# Your Supabase project URL
# Find this in: Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Your Supabase SERVICE ROLE KEY (NOT the anon key!)
# Find this in: Supabase Dashboard > Settings > API > Service Role Key
# WARNING: This key bypasses Row Level Security - keep it secret!
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx

# ============================================================================
# POSTGRESQL CREDENTIALS (for data import)
# ============================================================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/smart_calculator

# ============================================================================
# APPLICATION CREDENTIALS (for running the app)
# ============================================================================
# JWT secret for authentication (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Node environment
NODE_ENV=development

# ============================================================================
# MIGRATION NOTES
# ============================================================================
# 1. Use the SERVICE ROLE KEY, not the anon key!
# 2. Ensure PostgreSQL database exists before importing
# 3. Run migrations first: npm run migrate
# 4. Then run: npm run migration:export
# 5. Then run: npm run migration:import
# 6. Finally verify: npm run migration:verify
